tls:
  options:
    default:
      minVersion: VersionTLS12
      sniStrict: false
      cipherSuites:
        - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
        - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
        - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
        - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
        - TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305
        - TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305

http:
  middlewares:

    # Used by all routes. frameDeny is intentionally omitted — the onlineessl.in
    # ASP.NET apps use framesets internally and break if DENY is injected on sub-pages.
    # The apps set their own X-Frame-Options: DENY where appropriate.
    security-headers:
      headers:
        contentTypeNosniff: true
        browserXssFilter: true
        referrerPolicy: no-referrer-when-downgrade
        customResponseHeaders:
          X-Content-Type-Options: nosniff
        stsSeconds: 31536000
        stsIncludeSubdomains: true
        stsPreload: true

    # Restricts dashboard access to LAN only (172.22.66.0/26 = Traefik's subnet)
    allow-lan-only:
      ipAllowList:
        sourceRange:
          - "172.22.66.0/26"
          - "127.0.0.1/32"

    compress:
      compress: {}
